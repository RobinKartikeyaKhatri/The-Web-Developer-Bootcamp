// Printing numbers from 1 To 10

// To execute this code install node and run this command --- node <file_name>

let num = 1;

while (num <= 10) {
    console.log(num);
    num++;
}

// Printing each character in a string
let name = "Avinash";
let count = 0;

while (count < name.length) {
    console.log(name[count]);
    count++;
}